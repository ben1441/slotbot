import React from 'react';
import './Loader.scss'; // Make sure to create this CSS file with the provided styles

const Loader = () => {
    return (
        <div className="loader"></div>
    );
}

export default Loader;
